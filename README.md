# mesh_wx
uses openweathermap.org API to create weather report then send over a local mesh

runs on a raspberry pi via cron and connects to a Seed Studio Wio L1 Pro via USB

don't forget to put your own API key in
